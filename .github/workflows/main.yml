name: Build JAR on Push

on:
  push:
    branches:
      - my-new-branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

<<<<<<< HEAD
      - name: Compile Java
        run: |
          mkdir out
          javac -d out prog1.java
          jar --create --file app.jar -C out .
=======
      - name: Compile and package executable JAR
        run: |
          javac prog1.java
          jar cfe app-${{github.run_number}}.jar prog1 prog1.class
>>>>>>> my-new-branch

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
<<<<<<< HEAD
          name: app-jar
          path: app.jar
=======
          name: app-jar-${{github.run_number}}
          path: app-${{github.run_number}}.jar
>>>>>>> my-new-branch
